# Copy to .env (the installer does this for you) and fill in.
# The script auto-loads this file — no shell setup required.

# --- Channel posts (Power Automate Workflows webhook) ---
TEAMS_WEBHOOK_URL=

# --- Direct messages (Microsoft Graph, delegated device-code) ---
GRAPH_TENANT_ID=
GRAPH_CLIENT_ID=
